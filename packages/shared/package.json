{
  "name": "@subscription-tracker/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\" --max-warnings=0 && tsc -p tsconfig.json --noEmit",
    "test": "node -e \"console.log('No shared tests configured yet')\""
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
